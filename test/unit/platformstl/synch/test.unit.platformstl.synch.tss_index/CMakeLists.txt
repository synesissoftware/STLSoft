define_automated_test_program(test.unit.platformstl.synch.tss_index entry.cpp)
if(UNIX)
	target_compile_options(test.unit.platformstl.synch.tss_index PRIVATE -pthread -Wno-deprecated-copy)
	target_compile_definitions(test.unit.platformstl.synch.tss_index PRIVATE _REENTRANT)
	target_link_options(test.unit.platformstl.synch.tss_index PRIVATE -pthread)
endif()
